[project]
name = "Probe Station"
version = "0.1.0"
requires-python = ">=3.12"

[tool.ruff]
exclude = [
   "docs",
]

[tool.ruff.lint]
select = ["ALL"]

[tool.ruff.format]
docstring-code-format = true